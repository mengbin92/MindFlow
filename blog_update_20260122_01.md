# MindFlow 项目重大更新：主题与配置系统全新上线！

> 📅 **更新时间**：2026年1月22日
>
> 🎯 **版本**：v0.6.0
>
> 📝 **作者**：MindFlow Team

---

## 项目简介

**MindFlow** 是一款极简风格的开源 Markdown 编辑器，致力于为开发者和写作爱好者提供流畅的写作体验。项目采用现代化的技术栈和架构设计，支持多平台（桌面端、Web端、移动端），完全本地化运行，全方位保障用户隐私。

### 本次更新亮点

- 🎨 **完整主题系统** - 浅色/深色/自动三种主题模式
- ⚙️ **配置管理中心** - 可视化配置界面
- 💾 **双层持久化** - localStorage + 文件系统双重保障
- 🔤 **FiraCode 字体** - 专业编程字体集成
- 📤 **配置导入导出** - 灵活的配置管理
- 🔄 **实时预览** - 配置修改即时生效

---

## 本次更新内容详解

### 📋 Phase 6 完成情况 ✅

经过紧张有序的开发，MindFlow 项目已完成 **Phase 6: 主题与配置** 阶段，所有核心功能均已实现：

#### 已完成任务清单

| 任务 | 工作量 | 优先级 | 状态 |
|------|--------|--------|------|
| 主题系统架构 | 2天 | P0 | ✅ 已完成 |
| 深色/浅色主题 | 2天 | P0 | ✅ 已完成 |
| FiraCode 字体集成 | 1天 | P1 | ✅ 已完成 |
| 配置管理模块 | 2天 | P0 | ✅ 已完成 |
| 配置 UI 界面 | 2天 | P1 | ✅ 已完成 |
| 配置导入/导出 | 1天 | P2 | ✅ 已完成 |

#### 主要交付物

1. ✅ **配置管理系统**
   - Redux Toolkit 状态管理
   - 双层持久化机制
   - 完整的 CRUD 操作

2. ✅ **主题系统**
   - 浅色主题（Light）
   - 深色主题（Dark）
   - 自动主题（跟随系统）
   - CSS 变量架构

3. ✅ **设置对话框**
   - 外观设置
   - 编辑器设置
   - 自动保存设置
   - 配置管理功能

4. ✅ **Tauri 后端**
   - 配置文件读写
   - 导入/导出支持
   - JSON 格式存储

---

## 🏗️ 核心功能详解

### 1. 配置管理系统

基于 Redux Toolkit 的完整配置管理系统，支持配置的持久化和导入导出。

#### 🎯 设计目标

- 类型安全的配置管理
- 双层持久化机制
- 实时配置预览
- 完整的错误处理

#### 📦 配置结构

**AppConfig 接口定义**

```typescript
/**
 * 应用配置接口
 * @description 定义应用的所有可配置选项
 */
export interface AppConfig {
  /** 主题设置：light、dark 或 auto（跟随系统） */
  theme: 'light' | 'dark' | 'auto';

  /** 编辑器字体大小（像素） */
  fontSize: number;

  /** 编辑器字体系列 */
  fontFamily: string;

  /** Tab 键对应的空格数 */
  tabSize: number;

  /** 是否启用自动换行 */
  wordWrap: boolean;

  /** 是否显示行号 */
  lineNumbers: boolean;

  /** 是否启用自动保存 */
  autoSave: boolean;

  /** 自动保存延迟时间（毫秒） */
  autoSaveDelay: number;
}

/**
 * 默认应用配置
 * @description 应用首次启动时的默认配置
 */
export const DEFAULT_CONFIG: AppConfig = {
  theme: 'light',
  fontSize: 14,
  fontFamily: 'Fira Code',
  tabSize: 4,
  wordWrap: true,
  lineNumbers: true,
  autoSave: true,
  autoSaveDelay: 1000,
};
```

#### 🔄 状态管理

**Redux Slice 实现**

```typescript
/**
 * @fileoverview 配置管理状态管理
 * @description 使用Redux Toolkit管理应用配置状态
 */

import { createSlice, createAsyncThunk, PayloadAction } from '@reduxjs/toolkit';
import { invoke } from '@tauri-apps/api/tauri';

interface ConfigState {
  /** 当前配置 */
  config: AppConfig;

  /** 是否正在加载 */
  isLoading: boolean;

  /** 错误信息 */
  error: string | null;

  /** 最后保存时间 */
  lastSaved: Date | null;

  /** 是否已初始化 */
  isInitialized: boolean;
}

const configSlice = createSlice({
  name: 'config',
  initialState,
  reducers: {
    /** 更新配置 */
    updateConfig: (state, action: PayloadAction<Partial<AppConfig>>) => {
      state.config = { ...state.config, ...action.payload };
    },

    /** 设置主题 */
    setTheme: (state, action: PayloadAction<'light' | 'dark' | 'auto'>) => {
      state.config.theme = action.payload;
    },

    /** 设置字体大小 */
    setFontSize: (state, action: PayloadAction<number>) => {
      state.config.fontSize = action.payload;
    },

    // ... 更多 reducers
  },
  extraReducers: (builder) => {
    // 异步操作处理
    builder
      .addCase(saveConfig.fulfilled, (state, action) => {
        state.isLoading = false;
        state.config = action.payload;
        state.lastSaved = new Date();
      })
      .addCase(loadConfig.fulfilled, (state, action) => {
        state.isLoading = false;
        state.config = action.payload;
        state.isInitialized = true;
      });
  },
});
```

#### 💾 双层持久化

**localStorage + 文件系统**

```typescript
/**
 * 保存配置到文件系统
 */
export const saveConfig = createAsyncThunk(
  'config/saveConfig',
  async (config: AppConfig) => {
    try {
      await invoke('save_config', { config });
      // 同时保存到 localStorage 作为备份
      localStorage.setItem('mindflow-config', JSON.stringify(config));
      return config;
    } catch (error) {
      // 如果 Tauri 命令失败（例如在 Web 环境），只保存到 localStorage
      localStorage.setItem('mindflow-config', JSON.stringify(config));
      return config;
    }
  }
);

/**
 * 从文件系统加载配置
 */
export const loadConfig = createAsyncThunk(
  'config/loadConfig',
  async () => {
    try {
      const config = await invoke<AppConfig>('load_config', {});
      // 同步到 localStorage
      localStorage.setItem('mindflow-config', JSON.stringify(config));
      return config;
    } catch (error) {
      // 如果 Tauri 命令失败，从 localStorage 读取
      const savedConfig = localStorage.getItem('mindflow-config');
      if (savedConfig) {
        return { ...DEFAULT_CONFIG, ...JSON.parse(savedConfig) };
      }
      return { ...DEFAULT_CONFIG };
    }
  }
);
```

---

### 2. 主题系统

完整的 CSS 变量主题系统，支持浅色、深色和自动三种模式。

#### 🎨 CSS 变量架构

**主题变量定义**

```css
/* ==================== 浅色主题 ==================== */
:root {
  /* 文本颜色 */
  --color-primary: #213547;
  --color-secondary: #666;
  --color-muted: #999;

  /* 背景颜色 */
  --bg-primary: #ffffff;
  --bg-secondary: #f5f5f5;
  --bg-hover: rgba(0, 0, 0, 0.05);
  --bg-selected: rgba(33, 53, 71, 0.1);

  /* 编辑器颜色 */
  --editor-bg: #ffffff;
  --editor-text: #213547;
  --editor-gutter: #f5f5f5;
  --editor-selection: rgba(33, 53, 71, 0.15);

  /* 代码高亮 - 浅色 */
  --code-keyword: #0000ff;
  --code-string: #a31515;
  --code-comment: #008000;
  --code-function: #795e26;

  /* 字体 */
  --font-family-mono: 'Fira Code', 'Consolas', monospace;
  --font-size-base: 14px;
}

/* ==================== 深色主题 ==================== */
.theme-dark {
  /* 文本颜色 */
  --color-primary: #d4d4d4;
  --color-secondary: #999;
  --color-muted: #666;

  /* 背景颜色 */
  --bg-primary: #1e1e1e;
  --bg-secondary: #252526;
  --bg-hover: rgba(255, 255, 255, 0.1);
  --bg-selected: rgba(255, 255, 255, 0.15);

  /* 编辑器颜色 */
  --editor-bg: #1e1e1e;
  --editor-text: #d4d4d4;
  --editor-gutter: #252526;
  --editor-selection: rgba(255, 255, 255, 0.15);

  /* 代码高亮 - 深色 */
  --code-keyword: #569cd6;
  --code-string: #ce9178;
  --code-comment: #6a9955;
  --code-function: #dcdcaa;
}

/* ==================== 自动主题（跟随系统） ==================== */
@media (prefers-color-scheme: dark) {
  .theme-auto {
    /* 继承深色主题变量 */
    --color-primary: #d4d4d4;
    --editor-bg: #1e1e1e;
    /* ... */
  }
}
```

#### 🔄 主题切换

**ThemeToggle 组件**

```typescript
/**
 * 主题切换组件
 * @description 提供主题切换功能（浅色/深色/自动）
 */
export const ThemeToggle: React.FC<ThemeToggleProps> = ({ iconOnly = false }) => {
  const dispatch = useDispatch();
  const theme = useSelector((state: RootState) => state.config.config.theme);

  /**
   * 应用主题到 DOM
   */
  useEffect(() => {
    const root = document.documentElement;

    // 移除所有主题类
    root.classList.remove('theme-light', 'theme-dark', 'theme-auto');

    let themeToApply = theme;

    // 如果是 auto 模式，检测系统主题
    if (theme === 'auto') {
      const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
      themeToApply = prefersDark ? 'dark' : 'light';
    }

    // 添加对应的主题类
    root.classList.add(`theme-${themeToApply}`);

    // 保存到 localStorage
    localStorage.setItem('mindflow-theme', theme);

    // 监听系统主题变化
    if (theme === 'auto') {
      const mediaQuery = window.matchMedia('(prefers-color-scheme: dark)');
      const handleChange = (e: MediaQueryListEvent) => {
        root.classList.remove('theme-light', 'theme-dark');
        root.classList.add(`theme-${e.matches ? 'dark' : 'light'}`);
      };

      mediaQuery.addEventListener('change', handleChange);
      return () => mediaQuery.removeEventListener('change', handleChange);
    }
  }, [theme]);

  /**
   * 循环切换主题
   */
  const cycleTheme = () => {
    const themes: Array<'light' | 'dark' | 'auto'> = ['light', 'dark', 'auto'];
    const currentIndex = themes.indexOf(theme);
    const nextTheme = themes[(currentIndex + 1) % themes.length];
    handleThemeChange(nextTheme);
  };

  return (
    <button
      className="theme-toggle-icon"
      onClick={cycleTheme}
      title={`当前主题: ${getThemeLabel()} (点击切换)`}
    >
      <span className="theme-icon">{getThemeIcon()}</span>
    </button>
  );
};
```

#### 🎯 主题特性

- **浅色模式** - 明亮清爽，适合白天使用
- **深色模式** - 护眼舒适，适合夜间使用
- **自动模式** - 跟随系统设置，自动切换
- **平滑过渡** - 主题切换时的流畅动画
- **完整覆盖** - 所有 UI 组件的主题适配

---

### 3. 设置对话框

功能完整的设置界面，支持所有配置的可视化管理。

#### 🎨 界面设计

**SettingsDialog 组件**

```typescript
/**
 * 设置对话框组件
 * @description 提供应用配置的 UI 界面
 */
export const SettingsDialog: React.FC<SettingsDialogProps> = ({ isOpen, onClose }) => {
  const dispatch = useDispatch();
  const config = useSelector((state: RootState) => state.config.config);
  const [localConfig, setLocalConfig] = useState(config);
  const [hasChanges, setHasChanges] = useState(false);

  /**
   * 更新本地配置
   */
  const handleUpdateConfig = (updates: Partial<typeof config>) => {
    const newConfig = { ...localConfig, ...updates };
    setLocalConfig(newConfig);
    setHasChanges(true);
  };

  /**
   * 保存配置
   */
  const handleSave = () => {
    dispatch(updateConfig(localConfig));
    dispatch(saveConfig(localConfig) as any);
    setHasChanges(false);
    sendNotification({
      title: '配置已保存',
      body: '您的设置已成功保存',
    });
  };

  return (
    <div className="settings-dialog-overlay" onClick={handleCancel}>
      <div className="settings-dialog">
        {/* 标题栏 */}
        <div className="settings-dialog-header">
          <h2>设置</h2>
          <button className="settings-dialog-close" onClick={onClose}>✕</button>
        </div>

        {/* 内容区域 */}
        <div className="settings-dialog-content">
          {/* 主题设置 */}
          <div className="settings-section">
            <h3>外观</h3>
            <ThemeToggle />
          </div>

          {/* 编辑器设置 */}
          <div className="settings-section">
            <h3>编辑器</h3>
            <div className="settings-item">
              <label>字体大小</label>
              <input
                type="number"
                min="10"
                max="24"
                value={localConfig.fontSize}
                onChange={(e) => handleUpdateConfig({ fontSize: parseInt(e.target.value) || 14 })}
              />
            </div>
            {/* 更多设置项... */}
          </div>
        </div>

        {/* 底部按钮栏 */}
        <div className="settings-dialog-footer">
          <button onClick={handleCancel}>取消</button>
          <button onClick={handleSave} disabled={!hasChanges}>保存</button>
        </div>
      </div>
    </div>
  );
};
```

#### 📋 配置项

**外观设置**
- 主题选择（浅色/深色/自动）

**编辑器设置**
- 字体大小（10-24px）
- 字体系列（Fira Code、Consolas、Monaco 等）
- Tab 宽度（2-8 空格）
- 自动换行开关
- 行号显示开关

**自动保存设置**
- 启用/禁用自动保存
- 保存延迟设置（500-10000ms）

**配置管理**
- 导入配置文件
- 导出配置文件
- 重置为默认值

---

### 4. FiraCode 字体集成

专业的编程字体，支持连字特性。

#### 🔤 字体加载

**Google Fonts 集成**

```html
<!-- index.html -->
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Fira+Code:wght@400;500;600&display=swap" rel="stylesheet">
```

```css
/* CSS 字体定义 */
:root {
  --font-family-mono: 'Fira Code', 'Consolas', 'Monaco', 'Courier New', monospace;
}
```

#### ✨ 字体特性

- **连字支持** - `=>`, `!=`, `>=` 等符号自动合并
- **等宽设计** - 代码对齐完美
- **易读性强** - 清晰的字符区分
- **多字重** - 400、500、600 三种字重

---

### 5. Tauri 后端支持

完整的 Rust 后端实现，支持配置文件的读写和管理。

#### 🦀 Rust 实现

**配置结构定义**

```rust
/// 应用配置结构
#[derive(Debug, Serialize, Deserialize, Clone)]
#[serde(rename_all = "camelCase")]
pub struct AppConfig {
    pub theme: String,
    pub font_size: u32,
    pub font_family: String,
    pub tab_size: u32,
    pub word_wrap: bool,
    pub line_numbers: bool,
    pub auto_save: bool,
    pub auto_save_delay: u32,
}

/// 默认配置
impl Default for AppConfig {
    fn default() -> Self {
        Self {
            theme: "light".to_string(),
            font_size: 14,
            font_family: "Fira Code".to_string(),
            tab_size: 4,
            word_wrap: true,
            line_numbers: true,
            auto_save: true,
            auto_save_delay: 1000,
        }
    }
}

/// 获取配置文件路径
fn get_config_path() -> Result<std::path::PathBuf, String> {
    let config_dir = if let Ok(home) = env::var("HOME") {
        format!("{}/.config/mindflow", home)
    } else if let Ok(appdata) = env::var("APPDATA") {
        format!("{}\\MindFlow", appdata)
    } else {
        return Err("无法确定配置目录".to_string());
    };

    let config_path = std::path::PathBuf::from(config_dir);

    // 确保配置目录存在
    fs::create_dir_all(&config_path)
        .map_err(|e| format!("Failed to create config directory: {}", e))?;

    Ok(config_path.join("config.json"))
}
```

**Tauri 命令**

```rust
/// 保存配置到文件
#[tauri::command]
async fn save_config(config: AppConfig) -> Result<AppConfig, String> {
    let config_path = get_config_path()?;
    let config_json = serde_json::to_string_pretty(&config)
        .map_err(|e| format!("Failed to serialize config: {}", e))?;

    fs::write(&config_path, config_json)
        .map_err(|e| format!("Failed to write config file: {}", e))?;

    Ok(config)
}

/// 从文件加载配置
#[tauri::command]
async fn load_config() -> Result<AppConfig, String> {
    let config_path = get_config_path()?;

    if !config_path.exists() {
        // 如果配置文件不存在，返回默认配置
        return Ok(AppConfig::default());
    }

    let config_content = fs::read_to_string(&config_path)
        .map_err(|e| format!("Failed to read config file: {}", e))?;

    let config: AppConfig = serde_json::from_str(&config_content)
        .map_err(|e| format!("Failed to parse config file: {}", e))?;

    Ok(config)
}

/// 导出配置到指定文件
#[tauri::command]
async fn export_config(config: AppConfig, file_path: String) -> Result<(), String> {
    let expanded = expand_home(&file_path);
    let path_obj = Path::new(&expanded);

    // 确保父目录存在
    if let Some(parent) = path_obj.parent() {
        fs::create_dir_all(parent)
            .map_err(|e| format!("Failed to create directory: {}", e))?;
    }

    let config_json = serde_json::to_string_pretty(&config)
        .map_err(|e| format!("Failed to serialize config: {}", e))?;

    fs::write(&path_obj, config_json)
        .map_err(|e| format!("Failed to write export file: {}", e))?;

    Ok(())
}

/// 从指定文件导入配置
#[tauri::command]
async fn import_config(file_path: String) -> Result<AppConfig, String> {
    let expanded = expand_home(&file_path);
    let config_content = fs::read_to_string(&expanded)
        .map_err(|e| format!("Failed to read import file: {}", e))?;

    let config: AppConfig = serde_json::from_str(&config_content)
        .map_err(|e| format!("Failed to parse config file: {}", e))?;

    // 同时保存到默认配置文件
    let config_path = get_config_path()?;
    let config_json = serde_json::to_string_pretty(&config)
        .map_err(|e| format!("Failed to serialize config: {}", e))?;

    fs::write(&config_path, config_json)
        .map_err(|e| format!("Failed to write config file: {}", e))?;

    Ok(config)
}
```

#### 📁 配置文件位置

- **Linux/macOS**: `~/.config/mindflow/config.json`
- **Windows**: `%APPDATA%\MindFlow\config.json`

#### 📄 配置文件格式

```json
{
  "theme": "light",
  "fontSize": 14,
  "fontFamily": "Fira Code",
  "tabSize": 4,
  "wordWrap": true,
  "lineNumbers": true,
  "autoSave": true,
  "autoSaveDelay": 1000
}
```

---

## 📁 项目结构

### Phase 6 新增结构

```
packages/
├── desktop/
│   ├── src/
│   │   ├── store/
│   │   │   ├── configSlice.ts          # 配置管理 Redux slice（新增）
│   │   │   └── index.ts                # Store 配置（已更新）
│   │   ├── components/
│   │   │   └── Settings/               # 设置组件（新增）
│   │   │       ├── ThemeToggle.tsx     # 主题切换组件
│   │   │       ├── ThemeToggle.css     # 主题切换样式
│   │   │       ├── SettingsDialog.tsx  # 设置对话框
│   │   │       ├── SettingsDialog.css  # 设置对话框样式
│   │   │       └── index.ts            # 组件导出
│   │   ├── styles.css                  # 扩展主题变量（已更新）
│   │   └── App.tsx                     # 集成配置系统（已更新）
│   ├── index.html                      # 添加 Fira Code 字体（已更新）
│   └── src-tauri/
│       └── src/
│           └── lib.rs                  # 配置管理命令（已更新）
│
└── web/
    └── src/
        └── store/
            └── fileSystemSlice.ts      # 修复日期类型（已更新）
```

---

## 🔧 核心技术栈

### 主题与配置

| 技术 | 版本 | 用途 |
|------|------|------|
| Redux Toolkit | 最新 | 状态管理 |
| Tauri | 最新 | 桌面端框架 |
| CSS Variables | CSS3 | 主题系统 |
| TypeScript | 最新 | 类型安全 |
| Rust | 最新 | 后端实现 |

### 依赖管理

**@mindflow/desktop/package.json**

```json
{
  "dependencies": {
    "@reduxjs/toolkit": "^2.0.0",
    "@tauri-apps/api": "^1.5.0",
    "react-redux": "^9.0.0"
  }
}
```

---

## 📊 代码统计

### 文件统计

- **新增文件**: 6 个
- **修改文件**: 7 个
- **新增代码**: ~1200 行
- **新增样式**: ~450 行

### 组件统计

- **组件数**: 2 个
- **配置项**: 8 个
- **主题数**: 3 个
- **Redux Slices**: 1 个

---

## 🎯 使用指南

### 主题切换

**快速切换**
1. 点击侧边栏的主题图标
2. 在浅色/深色/自动之间循环切换

**详细设置**
1. 点击设置按钮（⚙️）
2. 在"外观"部分选择主题
3. 点击保存

### 配置编辑器

1. 打开设置对话框
2. 切换到"编辑器"标签
3. 调整各项配置：
   - 字体大小：10-24px
   - 字体：Fira Code、Consolas、Monaco 等
   - Tab 宽度：2-8 空格
   - 自动换行：开启/关闭
   - 行号显示：开启/关闭
4. 点击保存

### 配置自动保存

1. 打开设置对话框
2. 切换到"自动保存"标签
3. 启用/禁用自动保存
4. 调整保存延迟（500-10000ms）
5. 点击保存

### 导入/导出配置

**导出配置**
1. 打开设置对话框
2. 切换到"配置管理"标签
3. 点击"导出配置"
4. 选择保存位置
5. 配置导出为 JSON 文件

**导入配置**
1. 打开设置对话框
2. 切换到"配置管理"标签
3. 点击"导入配置"
4. 选择配置文件
5. 配置自动应用

**重置配置**
1. 打开设置对话框
2. 切换到"配置管理"标签
3. 点击"重置为默认"
4. 确认重置操作
5. 配置恢复默认值

---

## 🎯 下一步计划（Phase 7）

**Phase 7: 性能优化（Week 19-20）**

- [ ] 启动性能优化
- [ ] 大文件虚拟滚动
- [ ] 渲染性能优化
- [ ] 内存优化
- [ ] 性能监控

---

## 💡 项目亮点

### 1. 完整的主题系统

- 三种主题模式
- CSS 变量架构
- 跟随系统设置
- 平滑过渡动画

### 2. 专业的配置管理

- 可视化设置界面
- 双层持久化机制
- 导入/导出功能
- 实时预览效果

### 3. FiraCode 字体

- 编程连字支持
- Google Fonts 集成
- 多种字重选择

### 4. 类型安全

- TypeScript 全面覆盖
- Redux Toolkit 类型推断
- Rust 后端类型安全

---

## 🎯 总结

Phase 6 的完成标志着 MindFlow 拥有 **完整的主题与配置系统**：

✅ **主题系统** - 浅色/深色/自动三种模式
✅ **配置管理** - 可视化设置界面
✅ **双层持久化** - localStorage + 文件系统
✅ **FiraCode 字体** - 专业编程字体
✅ **导入/导出** - 灵活的配置管理
✅ **实时预览** - 配置即时生效

**文件统计**：
- 新增文件: 6 个
- 修改文件: 7 个
- 新增代码: ~1200 行
- 新增样式: ~450 行

现在，用户可以在 MindFlow 中：
- 🎨 自由切换主题模式
- ⚙️ 可视化配置所有设置
- 💾 享受双层持久化保障
- 📤 导入导出配置文件
- 🔤 使用专业的 FiraCode 字体

---

## 📚 相关文档

- [Phase 2 完成报告](./docs/Phase2-完成报告.md)
- [Phase 3 完成报告](./docs/Phase3-完成报告.md)
- [Phase 4 完成报告](./docs/Phase4-完成报告.md)
- [Phase 5 完成报告](./docs/Phase5-完成报告.md)
- [Phase 6 完成报告](./docs/Phase6-完成报告.md)
- [开发排期](./docs/开发排期.md)
- [技术方案设计](./docs/技术方案设计.md)
- [项目概览](./docs/项目概览.md)

---

## 🚀 立即体验

**桌面端**：
```bash
git clone https://github.com/your-org/mindflow.git
cd mindflow
pnpm install
pnpm --filter @mindflow/desktop dev
```

**Web 端**：
```bash
pnpm --filter @mindflow/web dev
```

---

**欢迎体验全新升级的 MindFlow！如有问题或建议，欢迎提交 Issue 或 PR。**

💬 **讨论**: [GitHub Discussions](https://github.com/your-org/mindflow/discussions)
🐛 **问题反馈**: [GitHub Issues](https://github.com/your-org/mindflow/issues)
📧 **联系我们**: team@mindflow.example.com

---

*MindFlow - 让写作回归纯粹，让配置更加灵活*

MIT License © 2026 MindFlow Team
