name: mindflow_export
description: MindFlow export services (HTML, PDF, image)
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.1.5
  pdf: ^3.10.8
  printing: ^5.13.4
  archive: ^4.0.0
  path: ^1.8.3
  share_plus: ^12.0.0
  web: ^1.1.0

dev_dependencies:
  flutter_lints: ^6.0.0
