name: mindflow_platform
description: MindFlow platform abstraction layer
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  mindflow_domain:
    path: ../mindflow_domain
  file_selector: ^1.0.4
  path: ^1.8.3
  web: ^1.1.0

dev_dependencies:
  flutter_lints: ^6.0.0
