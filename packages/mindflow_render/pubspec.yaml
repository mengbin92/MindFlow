name: mindflow_render
description: MindFlow markdown rendering layer
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  markdown: ^7.1.1
  flutter_markdown_plus: ^1.0.0

dev_dependencies:
  flutter_lints: ^6.0.0
